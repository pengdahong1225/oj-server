version: '3'
services:
  consul-server1:
    image: hashicorp/consul:1.11.2
    container_name: consul-server1
    restart: always
    volumes:
      - ./server1.json:/consul/config/server1.json:ro
      - ./certs/:/consul/config/certs/:ro
    networks:
      gvb:
        - ipv4_address: 172.19.0.2
    ports:
      - "8500:8500"
      - "8600:8600/tcp"
      - "8600:8600/udp"
    command: "agent -bootstrap-expect=3"

  consul-server2:
    image: hashicorp/consul:1.11.2
    container_name: consul-server2
    restart: always
    volumes:
      - ./server2.json:/consul/config/server2.json:ro
      - ./certs/:/consul/config/certs/:ro
    networks:
      gvb:
        - ipv4_address: 172.19.0.3
    command: "agent -bootstrap-expect=3"

  consul-server3:
    image: hashicorp/consul:1.11.2
    container_name: consul-server3
    restart: always
    volumes:
      - ./server3.json:/consul/config/server3.json:ro
      - ./certs/:/consul/config/certs/:ro
    networks:
      gvb:
        - ipv4_address: 172.19.0.4
    command: "agent -bootstrap-expect=3"

  consul-client:
    image: hashicorp/consul:1.11.2
    container_name: consul-client
    restart: always
    volumes:
      - ./client.json:/consul/config/client.json:ro
      - ./certs/:/consul/config/certs/:ro
    networks:
      gvb:
        - ipv4_address: 172.19.0.5
    command: "agent"

#  mysql:
#    image: mysql:8 # 使用的镜像
#    container_name: gvb-mysql # 启动的实例名称
#    environment:
#      MYSQL_ROOT_PASSWORD: 1225 # root用户密码
#    ports:
#      - "3306:3306" # 端口映射
#    volumes:
#      - ./.mysql-data:/var/lib/mysql # 将数据目录挂载到本地目录以进行持久化
#      - ./mysql-init:/docker-entrypoint-initdb.d # 启动脚本 docker-entrypoint-initdb.d机制自动执行`*.sql`
#    restart: always # 崩溃后重启
#    networks:
#      - gvb # 指定网络
#  redis:
#    image: redis:6
#    container_name: gvb-redis
#    ports:
#      - "6379:6379"
#    networks:
#      - gvb
#    volumes:
#      - ./.redis-data:/data # 持久化
networks:
  oj:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16
          gateway: 172.20.0.1